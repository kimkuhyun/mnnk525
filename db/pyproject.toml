[project]
name = "polaris-db"
version = "0.1.0"
description = "POLARIS DART 공시 수집·적재 (raw fetch → 청킹 → 3-DB)"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "neo4j>=6.2.0",
    "pymysql>=1.2.0",
    "python-dotenv>=1.0",
    "qdrant-client>=1.18.0",
]

[tool.uv]
package = false
